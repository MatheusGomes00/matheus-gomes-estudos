spring:
  application:
    name: msUser
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ms_users?useTimezone=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show_sql: false
  kafka:
    bootstrap-servers: localhost:9092
server:
  port: 8081